# 프로젝트: LDPlayer 게임 자동화 (화면 캡처 방식)

## 목적
LDPlayer 에뮬레이터 창이 떠 있는 내 모니터 화면을 30분마다 캡처하여 상태를 체크하고,
대기 상태일 경우 아이콘 클릭 → 팝업 확인 버튼 클릭을 자동화한다.
ADB를 사용하지 않고, 화면 캡처 + 이미지 매칭 + 마우스 클릭으로 동작한다.

## 기술 스택
- Python 3.11+
- pyautogui (화면 캡처, 마우스 이동/클릭)
- opencv-python (이미지 템플릿 매칭)
- Pillow (이미지 처리, pyautogui 의존성)
- schedule (30분 주기 실행)

## 동작 흐름
1. 30분마다 전체 화면을 스크린샷으로 캡처한다
2. 캡처한 이미지에서 "완료 상태(대기 상태)" 템플릿 이미지와 매칭을 시도하며, confidence threshold(기본 0.8) 이상인 매칭을 모두 찾는다
3. 매칭된 항목이 2개 이상이면 confidence 순으로 모두 순차적으로 처리한다 (1개만 처리하고 종료하지 않음)
4. 각 매칭 위치로 마우스를 이동해 아이콘을 클릭하고, 1초 대기 후 같은 위치를 한 번 더 클릭해 팝업을 닫는다 (별도의 확인 버튼 템플릿 매칭은 사용하지 않음)
5. 다음 매칭 처리 전 최소 2초 간격을 둔다

## 주석 작성 규칙
- 함수마다 목적을 설명하는 주석을 작성한다.
- 복잡한 로직은 "왜 이렇게 구현했는지"를 설명한다.
- 코드만 봐도 알 수 있는 내용은 주석으로 작성하지 않는다.
- 변수의 역할이 명확하지 않으면 주석을 추가한다.
- 한 줄 주석은 '#'을 사용한다.

## 코딩 규칙
- 예외 처리 필수 (이미지 매칭 실패, 화면 캡처 실패, 클릭 좌표 범위 오류 등)

## 작업 방식
- 새 기능을 추가하기 전에 먼저 계획을 설명하고 확인받은 후 코드를 작성한다
- 파일 하나씩 순차적으로 작업한다 (여러 파일 동시 대량 수정 금지)
- 테스트 가능한 함수 단위로 작게 나눠서 작성한다
- 실제 클릭 테스트 전, 캡처된 화면과 매칭 결과를 이미지로 저장해서 눈으로 확인할 수 있는 디버그 모드를 함께 만든다

## 하지 말아야 할 것
- requirements.txt 외의 불필요한 패키지 설치 금지
- 전역 변수 남발 금지
- 스크린샷/템플릿 이미지 파일을 assets/ 외의 경로에 생성 금지
- 매칭 confidence 체크 없이 무조건 클릭하는 로직 작성 금지